import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.farmer.app',
  appName: 'Farmer App',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
